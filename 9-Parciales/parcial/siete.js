function Mostrar()
{
	var notas;
	var sexo;
	var contadorNotas = 0;
	var contador = 0;

	notas = parseInt(prompt("Ingrese una nota"));
	sexo = prompt("Ingrese su sexo");

	while(contador < 10 && notas < 10 && notas > 0)
	{
		contador ++;
		notas = parseInt(prompt("Ingrese una nota"));
	}







}
